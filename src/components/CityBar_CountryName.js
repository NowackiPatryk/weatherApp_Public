import React from 'react';

const CityBar_CountryName = (props) => {
return ( 
        <h2 className = 'CityBar__CountryName'>
            {props.countryName}
        </h2>
        );
}
 
export default CityBar_CountryName;